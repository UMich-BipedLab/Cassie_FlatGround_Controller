% QUATERNION Implements a quaternion object
% 
%  Syntax:
%    q = Quaternion
%    q = Quaternion.identity
%    q = Quaternion(w, x, y, z)
%    q = Quaternion(quaternion)
% 
%  where,
%    w, x, y, z = Numeric scalars
%    quaternion = A 4x1 numeric matrix [w; x; y; z]
%
%    Reference page in Doc Center
%       doc Quaternion
%
%